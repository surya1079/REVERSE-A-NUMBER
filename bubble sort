class Solution:
    def bubbleSort(self,arr):
        # code here
        n=len(arr)
        for i in range(n-1,-1,-1):
            for j in range(i):
                if arr[j]>arr[i]:
                    arr[i],arr[j]=arr[j],arr[i]
        return arr            
